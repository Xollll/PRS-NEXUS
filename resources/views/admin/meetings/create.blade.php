@extends('layouts.app', ['title' => 'PRS NEXUS | Schedule meeting'])

@section('content')
    <section class="flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-blue-700">Events</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">Schedule meeting</h1><p class="mt-2 text-sm text-slate-600">Publish the essential details of a PRS meeting.</p></div><a href="{{ route('admin.meetings.index') }}" class="ui-button-secondary">Back to meetings</a></section>
    <form method="POST" action="{{ route('admin.meetings.store') }}" class="mt-6 ui-panel max-w-3xl">@csrf<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label for="title" class="ui-label">Meeting title</label><input id="title" name="title" required class="ui-input" value="{{ old('title') }}">@error('title')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="meeting_date" class="ui-label">Meeting date</label><input id="meeting_date" name="meeting_date" type="date" required class="ui-input" value="{{ old('meeting_date') }}">@error('meeting_date')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="location" class="ui-label">Location <span class="font-normal text-slate-500">(optional)</span></label><input id="location" name="location" class="ui-input" value="{{ old('location') }}">@error('location')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label for="summary" class="ui-label">Summary <span class="font-normal text-slate-500">(optional)</span></label><textarea id="summary" name="summary" rows="5" class="ui-textarea">{{ old('summary') }}</textarea>@error('summary')<p class="ui-error-text">{{ $message }}</p>@enderror</div></div><div class="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('admin.meetings.index') }}" class="ui-button-secondary text-center">Cancel</a><button class="ui-button-primary">Save meeting</button></div></form>
@endsection
