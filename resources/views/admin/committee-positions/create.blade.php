@extends('layouts.app', ['title' => 'PRS NEXUS | Add committee position'])

@section('content')
    <section class="flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-blue-700">Organization</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">Add committee position</h1><p class="mt-2 text-sm text-slate-600">Define a leadership role that can be assigned to a PRS member.</p></div><a href="{{ route('admin.committee-positions.index') }}" class="ui-button-secondary">Back to positions</a></section>
    <form method="POST" action="{{ route('admin.committee-positions.store') }}" class="mt-6 ui-panel max-w-3xl">@csrf<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label for="title" class="ui-label">Position title</label><input id="title" name="title" required class="ui-input" value="{{ old('title') }}">@error('title')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="category" class="ui-label">Category</label><input id="category" name="category" required class="ui-input" value="{{ old('category', 'executive') }}">@error('category')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="sort_order" class="ui-label">Display order</label><input id="sort_order" name="sort_order" type="number" min="0" required class="ui-input" value="{{ old('sort_order', 0) }}">@error('sort_order')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label for="description" class="ui-label">Description <span class="font-normal text-slate-500">(optional)</span></label><textarea id="description" name="description" rows="5" class="ui-textarea">{{ old('description') }}</textarea>@error('description')<p class="ui-error-text">{{ $message }}</p>@enderror</div></div><div class="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('admin.committee-positions.index') }}" class="ui-button-secondary text-center">Cancel</a><button class="ui-button-primary">Save position</button></div></form>
@endsection
