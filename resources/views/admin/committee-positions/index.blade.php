@extends('layouts.app', ['title' => 'SiswaSphere | Committee positions'])

@section('content')
    <section class="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-blue-700">Organization</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">Committee positions</h1><p class="mt-2 text-sm text-slate-600">Maintain the PRS leadership roles available to members.</p></div><a href="{{ route('admin.committee-positions.create') }}" class="ui-button-primary w-full sm:w-auto">Add position</a></section>
    <section class="mt-6 ui-panel"><form method="GET" action="{{ route('admin.committee-positions.index') }}" class="flex flex-col gap-3 sm:flex-row"><div class="flex-1"><label for="q" class="sr-only">Search committee positions</label><input id="q" name="q" value="{{ $query }}" class="ui-input" placeholder="Search by title, category, or description"></div><button class="ui-button-secondary">Search</button>@if($query !== '')<a href="{{ route('admin.committee-positions.index') }}" class="ui-button-secondary text-center">Clear</a>@endif</form></section>
    <section class="mt-6 ui-table-wrap"><table class="ui-table"><thead><tr><th>Position</th><th>Category</th><th>Order</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($committeePositions as $position)<tr><td><p class="font-semibold text-slate-900">{{ $position->title }}</p>@if($position->description)<p class="mt-1 max-w-xl text-sm text-slate-500">{{ $position->description }}</p>@endif</td><td>{{ $position->category }}</td><td>{{ $position->sort_order }}</td><td><div class="flex justify-end gap-3"><a href="{{ route('admin.committee-positions.edit', $position) }}" class="text-sm font-semibold text-blue-700 hover:underline">Edit</a><form method="POST" action="{{ route('admin.committee-positions.destroy', $position) }}" data-confirm="Remove this committee position? Members assigned to it will remain members without that position.">@csrf @method('DELETE')<button class="text-sm font-semibold text-red-700 hover:underline">Delete</button></form></div></td></tr>@empty<tr><td colspan="4"><x-empty-state title="No committee positions found" description="Create a position to define the PRS leadership structure."><a href="{{ route('admin.committee-positions.create') }}" class="ui-button-secondary">Add position</a></x-empty-state></td></tr>@endforelse</tbody></table></section>
    @if($committeePositions->hasPages())<div class="mt-6">{{ $committeePositions->links() }}</div>@endif
@endsection
