@extends('layouts.app', ['title' => 'SiswaSphere | Add activity'])

@section('content')
    <section class="flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-blue-700">Events</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">Add activity</h1><p class="mt-2 text-sm text-slate-600">Create a PRS activity and set its current delivery status.</p></div><a href="{{ route('admin.activities.index') }}" class="ui-button-secondary">Back to activities</a></section>
    <form method="POST" action="{{ route('admin.activities.store') }}" class="mt-6 ui-panel max-w-3xl">@csrf<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label for="title" class="ui-label">Activity title</label><input id="title" name="title" required class="ui-input" value="{{ old('title') }}">@error('title')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="activity_date" class="ui-label">Activity date</label><input id="activity_date" name="activity_date" type="date" required class="ui-input" value="{{ old('activity_date') }}">@error('activity_date')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div><label for="status" class="ui-label">Status</label><select id="status" name="status" required class="ui-select"><option value="planned" @selected(old('status', 'planned') === 'planned')>Planned</option><option value="ongoing" @selected(old('status') === 'ongoing')>Ongoing</option><option value="completed" @selected(old('status') === 'completed')>Completed</option></select>@error('status')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label for="location" class="ui-label">Location <span class="font-normal text-slate-500">(optional)</span></label><input id="location" name="location" class="ui-input" value="{{ old('location') }}">@error('location')<p class="ui-error-text">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label for="description" class="ui-label">Description <span class="font-normal text-slate-500">(optional)</span></label><textarea id="description" name="description" rows="5" class="ui-textarea">{{ old('description') }}</textarea>@error('description')<p class="ui-error-text">{{ $message }}</p>@enderror</div></div><div class="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('admin.activities.index') }}" class="ui-button-secondary text-center">Cancel</a><button class="ui-button-primary">Save activity</button></div></form>
@endsection
