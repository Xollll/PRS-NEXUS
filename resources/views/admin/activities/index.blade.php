@extends('layouts.app', ['title' => 'PRS NEXUS | Activities'])

@section('content')
    <section class="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-blue-700">Events</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">Activities</h1><p class="mt-2 text-sm text-slate-600">Coordinate PRS activities and keep their status current.</p></div><a href="{{ route('admin.activities.create') }}" class="ui-button-primary w-full sm:w-auto">Add activity</a></section>
    <section class="mt-6 ui-panel"><form method="GET" action="{{ route('admin.activities.index') }}" class="grid gap-3 sm:grid-cols-[1fr_11rem_auto_auto]"><div><label for="q" class="sr-only">Search activities</label><input id="q" name="q" value="{{ $query }}" class="ui-input" placeholder="Search title, venue, or description"></div><div><label for="status" class="sr-only">Activity status</label><select id="status" name="status" class="ui-select"><option value="">All statuses</option><option value="planned" @selected($status === 'planned')>Planned</option><option value="ongoing" @selected($status === 'ongoing')>Ongoing</option><option value="completed" @selected($status === 'completed')>Completed</option></select></div><button class="ui-button-secondary">Apply</button>@if($query !== '' || $status !== '')<a href="{{ route('admin.activities.index') }}" class="ui-button-secondary text-center">Clear</a>@endif</form></section>
    <section class="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($activities as $activity)<x-card><div class="flex items-start justify-between gap-3"><x-badge :variant="$activity->status === 'completed' ? 'success' : ($activity->status === 'ongoing' ? 'info' : 'upcoming')">{{ ucfirst($activity->status) }}</x-badge><div class="flex gap-3"><a href="{{ route('admin.activities.edit', $activity) }}" class="text-sm font-semibold text-blue-700 hover:underline">Edit</a><form method="POST" action="{{ route('admin.activities.destroy', $activity) }}" data-confirm="Remove this activity? This action cannot be undone.">@csrf @method('DELETE')<button class="text-sm font-semibold text-red-700 hover:underline">Delete</button></form></div></div><h2 class="mt-4 text-lg font-semibold text-slate-900">{{ $activity->title }}</h2><p class="mt-2 text-sm text-slate-500">{{ $activity->activity_date->format('d M Y') }} · {{ $activity->location ?: 'Venue to be confirmed' }}</p>@if($activity->description)<p class="mt-3 text-sm leading-6 text-slate-600">{{ $activity->description }}</p>@endif</x-card>@empty<x-empty-state class="md:col-span-2 xl:col-span-3" title="No activities found" description="Add an activity to make it visible to PRS members."><a href="{{ route('admin.activities.create') }}" class="ui-button-secondary">Add activity</a></x-empty-state>@endforelse</section>
    @if($activities->hasPages())<div class="mt-6">{{ $activities->links() }}</div>@endif
@endsection
