@extends('layouts.app', ['title' => 'PRS NEXUS | Edit member'])
@section('content')
<a href="{{ route('admin.members.show', $member) }}" class="text-sm font-semibold text-blue-700 hover:underline">Back to member profile</a><section class="mx-auto mt-5 max-w-4xl ui-panel"><p class="text-sm font-semibold text-blue-700">Organization</p><h1 class="mt-1 text-3xl font-bold text-slate-900">Edit {{ $member->full_name }}</h1><p class="mt-2 text-sm text-slate-600">Update member information, PRS role, or portal access.</p><form action="{{ route('admin.members.update', $member) }}" method="POST" class="mt-8 space-y-8">@csrf @method('PUT')<section><h2 class="text-lg font-semibold text-slate-900">Personal and academic information</h2><div class="mt-4 grid gap-4 sm:grid-cols-2"><div class="sm:col-span-2"><x-form-input label="Full name" name="full_name" :value="$member->full_name" :required="true"/></div><x-form-input label="Matric number" name="matric_no" :value="$member->matric_no" :required="true"/><x-form-input label="Email address" name="email" type="email" :value="$member->email" :required="true"/><x-form-input label="Phone" name="phone" :value="$member->phone"/><x-form-input label="Programme" name="programme" :value="$member->programme"/></div></section><section class="border-t border-slate-200 pt-6"><h2 class="text-lg font-semibold text-slate-900">PRS membership</h2><div class="mt-4 grid gap-4 sm:grid-cols-2"><div><label for="committee_position_id" class="ui-label">Committee position</label><select id="committee_position_id" name="committee_position_id" class="ui-select"><option value="">No assigned position</option>@foreach($committeePositions as $position)<option value="{{ $position->id }}" @selected(old('committee_position_id', $member->committee_position_id) == $position->id)>{{ $position->title }}</option>@endforeach</select></div><x-form-input label="Custom role title" name="role_title" :value="$member->role_title" help="Use only when no listed committee position applies."/><div><label for="status" class="ui-label">Membership status</label><select id="status" name="status" class="ui-select"><option value="active" @selected(old('status', $member->status) === 'active')>Active</option><option value="inactive" @selected(old('status', $member->status) === 'inactive')>Inactive</option></select></div></div></section><section class="border-t border-slate-200 pt-6"><h2 class="text-lg font-semibold text-slate-900">Portal access</h2><p class="mt-1 text-sm text-slate-500">Leave password fields empty to keep the current password.</p><div class="mt-4 grid gap-4 sm:grid-cols-2"><div><label for="password" class="ui-label">New portal password</label><input id="password" type="password" name="password" autocomplete="new-password" class="ui-input"></div><div><label for="password_confirmation" class="ui-label">Confirm new password</label><input id="password_confirmation" type="password" name="password_confirmation" autocomplete="new-password" class="ui-input"></div></div></section><div class="flex flex-wrap gap-3 border-t border-slate-200 pt-6"><button class="ui-button-primary">Save changes</button><a href="{{ route('admin.members.show', $member) }}" class="ui-button-secondary">Cancel</a></div></form></section>
@endsection
